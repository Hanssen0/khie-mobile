export type Screen = "home" | "receive" | "send" | "khie" | "trust" | "settings" | "scanner";

export type Onboarding =
  | "start"
  | "create"
  | "confirm"
  | "password"
  | "restore"
  | "trust";
